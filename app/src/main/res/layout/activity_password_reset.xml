<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".PasswordReset">

    <include layout="@layout/content_password_reset" />

    <TextView
        android:id="@+id/reset_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/toolbar"
        android:fontFamily="@font/nunito_bold"
        android:text="Please Enter your registered mail Address below.\nWe will send a link to your mail and you can reset ur password"
        android:textSize="19sp"

        ></TextView>

    <EditText
        android:id="@+id/reset_input_mail"
        android:layout_width="292dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/reset_text"
        android:layout_alignParentRight="true"
        android:layout_marginStart="17dp"
        android:layout_marginLeft="17dp"
        android:layout_marginTop="39dp"
        android:layout_marginEnd="5dp"
        android:layout_marginRight="5dp"
        android:layout_marginBottom="5dp"
        android:layout_toEndOf="@+id/mail_image"
        android:layout_toRightOf="@+id/mail_image"
        android:fontFamily="@font/nunito_bold"
        android:hint="abc@gmial.com"
        android:inputType="textEmailAddress"></EditText>

    <ImageView
        android:id="@+id/mail_image"
        android:layout_width="53dp"
        android:layout_height="52dp"
        android:layout_below="@id/reset_text"
        android:layout_alignParentLeft="true"
        android:layout_marginLeft="16dp"
        android:layout_marginTop="39dp"
        android:src="@drawable/email">

    </ImageView>

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        app:popupTheme="@style/AppTheme.PopupOverlay" />

    <Button
        android:id="@+id/reset_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/reset_input_mail"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp"
        android:fontFamily="@font/nunito_bold"
        android:background="@drawable/grad3"
        android:textColor="#ffffff"
        android:text="Submit" />

</RelativeLayout>